<h1>Hi, <%= @current_user.email %>!</h1>

<% if @accounts %>
	<ul>
	<% @accounts.each do |account| %>
		<li><%= link_to account.name, account_path(account) %>
	<% end %>
	</ul>
<% else %>

<% end %>

<%= link_to 'Add Account', new_account_url %>